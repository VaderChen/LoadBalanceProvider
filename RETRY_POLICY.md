# 連線與重試

服務會在可恢復的故障期間盡量保留用戶端連線，但不會無限重送，也不會把失敗偽裝成成功。

## 發生暫時故障時

1. 請求第一次選定 Provider 與模型後，通常沿用相同來源；符合安全條件的容量失敗可改選其他帳號。成功改綁後重設新來源的工作嘗試額度，短時間探測總量與等待限制仍共用。
2. 尚未向用戶端送出有效內容時，服務保留待送資料、暫存初始化事件，並向用戶端送出保活心跳。
3. 收到可重試的過載或伺服器錯誤後，依冷卻時間等待再嘗試。
4. 成功後轉送正常回應；等待或嘗試次數用盡則回報錯誤。

每次上游推論嘗試另有 1–2 秒隨機等待，以分散瞬間請求。心跳只表示連線仍在，不代表模型已有進展。

收到完整上游 SSE 事件（包含 `keepalive`、其他心跳及註解心跳）會重設串流閒置計時；代理自行送給用戶端的保活心跳不會重設上游計時。持續收到上游心跳時可繼續等待，但不視為內容交付或請求成功；上游失敗事件仍依失敗流程處理。冷卻等待預算以後台「累計冷卻等待上限」為準，不另套固定 30 秒上限。

### Codex 即時串流

Codex 來源的串流 Responses 隨上游產出轉送文字及工具事件，不等待整筆回應完成。尚未送達有效內容前，可依既有預算重試；一旦內容送出，便不在同一條串流內重新生成，避免混合回應。

只有容量失敗、尚未送達內容、沒有金鑰或請求強制指定來源，而且具備自包含歷史時，才允許改選其他帳號。備援會移除舊帳號專用的推理項目，保留訊息與工具結果；僅有 `previous_response_id`、未展開的參照或缺少原始呼叫的工具結果，不會跨帳號接續。其他伺服器錯誤仍沿用原來源。

同回合的不同請求依序處理，改綁會同步更新持久化紀錄；原始 response ID 仍屬於原帳號，不會被重新指向其他帳號。

整筆成功後才送出的緩衝模式目前未啟用，該模式的 2 MiB 上限不再限制此路徑。即時串流途中仍可能失敗；已由上游執行的操作不保證沒有副作用。

串流結束時主動關閉上游 body，紀錄開啟／關閉的活躍串流數與 Go goroutine 數，供排查長時間執行後的資源累積；這些數字不等同作業系統 TCP 連線數。

正文中出現 `[DONE]` 不視為結束，必須是完整的串流控制事件。下游寫入或 Flush 失敗會停止交付並關閉上游，不計為正常完成，也不因此懲罰 Provider。選定來源時立即保留併發名額，請求離開時歸還；同一來源的設定重載不會把活躍數重設為零。

### 跨重連限制

已驗證呼叫端的 Responses 請求，若帶有穩定對話識別，會以完整請求的雜湊共用重試次數、累計等待與原 Provider／模型。相同請求正在處理時不再並行送往上游；額度用盡時適用冷卻規則。已交付完整工具呼叫後失敗，才觸發工具防重播封鎖；文字、推理摘要及未完成工具參數不因此封鎖重連，也不自動補滿額度。

失敗紀錄保留 10 分鐘，成功後清除；僅保存在目前程序記憶體，不保存提示詞或工具內容，服務重啟後不保留。不同使用者、新回合識別或不同完整請求（包含新的工具結果）分開計算；沒有穩定識別的獨立請求不只依文字判斷重送。

紀錄保留時間不是禁止重試時間。上游嘗試額度用盡後，代理暫停 30 秒；之後相同請求只獲得一次恢復探測，不重新取得整套重試額度。符合上述容量備援條件時可探測其他來源，否則仍使用原 Provider。探測失敗再暫停 30 秒，探測仍須遵守上游較長的冷卻。已交付完整工具呼叫的失敗不適用這個放行規則。用戶端顯示的「重新連線 1/5」與代理內部嘗試次數不同，不能直接對照。

尚未送出內容的容量拒絕會退回工作重試額度；不代表上游未消耗資源或退回帳號用量。退款不刪除實際探測次數、已嘗試來源、輪數及等待紀錄。每個 HTTP 請求的嘗試次數上限與上游冷卻仍保留。

容量備援成功保存不同 Provider 的配對後，新來源的工作嘗試次數由零開始，接著計入這次派送；改綁失敗或仍是同一 Provider 則不重設。不清除短時間探測次數、已嘗試來源與等待紀錄，也不直接延長目前 HTTP 請求的普通重試迴圈。

相同請求另外共用 30 秒探測窗口，上限為工作嘗試額度與 3 兩者的較大值；額度退款也計入實際探測次數。窗口到期後才重新開放探測，重連不延長窗口。已允許容量備援時略過工作額度的 30 秒冷卻，但不跳過探測窗口與候選來源的冷卻。沒有任何嘗試或排隊的空紀錄可清除，成功後則清除整筆紀錄。

相同請求重連時若遇到代理額度冷卻或探測窗口限制，代理會在等待上限內保留連線、送出心跳，並在冷卻到期後自動重新取得恢復探測資格，不要求 Agent 解讀「稍後重試」文字。等待期間不占上游併發名額、不增加實際嘗試次數；取得資格後仍須通過原 Provider 綁定、工作額度與上游冷卻檢查。工作額度用盡後仍只放行一次探測，不補滿整套重試。

若尚未交付內容、沒有待處理的容量改綁，且可重試的伺服器錯誤已耗盡工作額度，可承接一次下一冷卻窗口的同連線恢復探測。使用紀錄跨相同請求的重連保留，不會每條連線重新發放。等待扣除本連線剩餘的重試等待預算，保留原 Provider，並受回合恢復總量與模型共用探測限制。探測再次失敗、等待不足、取消或下游斷線即結束，不繼續循環。

### 回合與模型共用保護

- **回合恢復總量**：對具有穩定且已驗證回合識別的 Responses 請求，最多 6 次恢復派送、2 次成功切換帳號。換帳號重設的只有該來源工作額度，不重設這兩個上限。正常新請求的首次派送不算恢復重試。
- **失敗來源保留**：恢復紀錄從第一次失敗或恢復派送起保留固定 10 分鐘；重連、換帳號與 30 秒探測窗口到期都不重設期限。備援選路不再回訪期間內已失敗的來源；原本已綁定來源仍可依冷卻規則探測。紀錄到期後才解除這層限制。
- **回合識別邊界**：使用呼叫端身分、對話識別與提問回合標頭隔離；沒有提問回合標頭時，限制套用到同一對話的上述保留期間。新工具結果雖有自己的請求額度，仍共用回合恢復總量。無穩定識別的請求不保證有回合層級保護。
- **模型共用探測**：同一 Provider 類型、上游位址及模型，在 30 秒內有兩個不同帳號回報容量或可重試伺服器錯誤時，共用冷卻 30 秒。到期只放行一個探測，其餘請求有限等待；探測成功解除，失敗再冷卻。既有在途請求不強制取消，帳號配額故障不觸發這項保護。
- **原配對不變**：共用探測不把綁定請求移給其他帳號；候選來源自己的較長冷卻仍有效。限制紀錄只存在目前服務程序，不能跨服務實例協調，重啟也不保留。

這些限制用來減少故障時的請求放大，不保證每個回合都能完成。達到回合恢復上限會明確回報，不會偽裝成功；也不應藉更換回合識別規避限制。

剩餘冷卻超過等待上限時，先等待本次可用額度，不立即拒絕，也不提前向上游送出。等待結束仍未到期，會回報剩餘冷卻秒數；代理額度節流在尚未開始串流時回 HTTP 429 與 `Retry-After`，已開始 SSE 則使用失敗終止事件。設定為 0 時仍不等待。暫時節流不送出完成型助理訊息，因為完成事件會結束這次回應，文字中的等待秒數不是 Agent 的重試指令。客戶端是否繼續重連仍取決於它的重試策略與剩餘次數。

來源冷卻等待耗盡、但共用探測窗口尚未到期時，相同請求的重連會在入口有限等待窗口恢復。窗口到期後重設等待紀錄，再檢查原來源是否可用；不因重連延長期限，也不補滿工作嘗試額度。若上游要求的冷卻長於數次有限等待的總時間，用戶端仍可能耗盡重連次數，這不是無限自動恢復保證。

工具防重播封鎖也以完成型助理通知收尾，提醒確認工具結果並提出新的接續請求；不代表原任務成功。完成判準包含工具參數完成、自訂工具輸入完成及完成的工具項目事件，並追蹤跨寫入的完整事件邊界。下游接受位元組不代表工具一定已執行；代理採保守防重播處理。

這是限制上游重送，不是成功回應快取或「工具恰好執行一次」保證，也無法阻止用戶端自行發出 HTTP 請求。請勿以任意修改請求內容規避失敗後的暫停期間。

## 等待方式

| 情況 | 處理 |
| --- | --- |
| 上游提供有效的 `Retry-After` | 包含 HTTP 200 的 SSE 失敗也保留標頭提示；標頭與串流事件取較長等待，超過等待預算時結束，不提前重送。 |
| 暫時過載且無等待提示 | 依 2、4、8、16 秒遞增，加入隨機偏移。 |
| 一般可重試伺服器錯誤 | 預設冷卻 30 秒。 |
| 原 Provider 併發滿載 | 在等待預算內重新檢查容量，不改送其他來源。 |
| 配額耗盡 | 依可辨識的上游配額恢復時間冷卻。 |
| 格式、上下文或權限問題 | 不把問題當成一般過載無限重試；依錯誤回報或隔離來源。 |

同一冷卻窗口的並發失敗不會反覆升級退避；若有更長的有效上游等待提示，仍會延長至必要期限。模型與帳號層級的冷卻分開處理。

## 可調整項目

在「設定 > 進階」調整：

- **伺服器錯誤冷卻**：預設 30 秒，可設 1–300 秒。
- **累計冷卻等待**：預設 30 秒，可設 0–300 秒；0 表示不等待。首次送出前的排隊與失敗後的退避分開累計，各自套用此上限。重連入口等待代理額度冷卻另有同值的單次上限，因此預設單次 HTTP 請求最多 90 秒等待，不含上游執行時間；一般未遇到入口冷卻的請求仍最多 60 秒。
- **配額冷卻保存**：預設開啟，保存仍有至少 5 分鐘的長期配額冷卻，供重啟後恢復。

追蹤項目保留期間，兩份等待預算跨相同請求的重連累計；探測窗口到期或工作額度冷卻後開放恢復探測時重設。容量退款本身不重設等待。首次排隊不吃掉失敗後的退避空間，未曾送出的請求也能在窗口到期後重新排隊；單次請求仍不會無限等待。

等待預算不是整段生成的總時間，也不包含每次上游生成耗時。既有的 `retry_count` 與固定來源重試上限仍限制實際嘗試；容量備援另遵守選路輪數／來源數限制，已嘗試來源跨重連保留。取消請求會停止後續等待。

重連入口依精確的冷卻到期時間等待，不依顯示用的整數秒重新計時；輪詢或重連不延長期限。取消請求或下游寫入失敗即停止後續探測，工具防重播與相同請求仍在處理的情況不會進入自動等待。

## 不能保證的情況

- 已轉送內容後，不在原串流接上另一次生成；跨重連重試文字或推理仍可能重複顯示。此策略假設客戶端不執行尚未完成的工具呼叫。
- 上游持續失敗、網路斷線、用戶端逾時或服務重新啟動，仍可能需要重新連線。
- 代理不保證工具恰好執行一次；上游即使未成功完成，也可能已產生用量或其他副作用。
- 暫時故障冷卻不等於帳號剩餘配額，也不代表減少併發一定能消除上游錯誤。

遇到重複斷線時，請保留發生時間、錯誤訊息及 request ID，供管理員對照 `service.log`。不要提供 Token、API Key 或完整私人對話。部署要求請見 [部署手冊](DEPLOY.md#串流連線與反向代理)。
