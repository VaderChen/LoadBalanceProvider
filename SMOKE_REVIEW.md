# 三輪整體檢視與 SMOKE 紀錄

日期：2026-09-08。環境：Go 1.27.0、macOS arm64。

本次維持每回合的 Provider 綁定原則，不因額度均衡而搬動進行中的回合。檢查涵蓋來源選擇、用量統計、回合保存、重試、串流交付與資源回收。

## 第一輪：用量與錯誤等待

檢視後以四個核心套件的測試建立基線，確認並修正：

- 日初基準與一般用量觀測同時間戳時，一般觀測被誤判為舊資料，當日消耗可能維持零。現在只拒絕真正較舊的資料；相同時間戳仍由低水位規則避免重複計數。
- 日終擷取後仍收到新觀測時，圖表可能繼續採用舊的日終剩餘量。現在日終紀錄隨已知最新觀測更新，延遲到達的舊快照不能覆蓋新值。
- 舊測試預期的 2 分鐘等待，與 1 小時後才恢復的額度窗口矛盾。校正測試，並補上較長 Retry-After 不能被縮短的斷言；沒有放寬重試政策。

日終問題先以新測試重現失敗，修正後四個核心套件共 326 項測試通過，含子測試。

## 第二輪：重載、併發與配對

檢查設定重載、名額取得與歸還、配對保存及容量備援，確認並修正：

- 命中記憶體配對時，原本略過已保存配對的帳號指紋驗證。現在仍核對保存的來源身分，避免舊 response ID 被送往更換後的帳號。
- 已保存配對過期後，記憶體快取不能讓它重新生效；有效快照的歷史與回應內容仍保留。

新增測試先重現帳號驗證與到期判斷的失敗。修正後，三個新增情境在 race 檢查下連跑三次通過；其中併發情境每次包含 8 個工作者、800 次名額申請及 100 次設定重載，驗證沒有超額接收、漏還名額或遺失成功紀錄。

四個核心套件共 329 項測試在 race 檢查下通過，含子測試。

## 第三輪：反覆故障與恢復

重新檢查重連額度、工具防重播、串流收尾與帳號用量輪詢，確認並修正：

- 完整工具呼叫只出現在 `response.completed.output` 時，原本未被辨識為已交付。現在納入完成事件中的工具項目；文字、推理、標示生成中的工具及未寫完的 SSE 事件仍不觸發工具封鎖。
- 到期配對被資料庫清理後，可能被誤當成舊版未保存快照。補上記憶體持久化標記，並在保存、恢復、改綁及同來源快取更新時維持標記，避免清理後復活。

新增工具情境先重現失敗，修正後完成以下驗證：

- 工具完成、文字、推理、未完成呼叫及部分寫入，在 race 檢查下連跑三次通過。
- 串流正常結束、容量拒絕及下游斷線，各跑 100 次，再整組連跑三次，共 900 次。確認上游只關閉一次、活躍串流數回到原值、測試上游的產生者 goroutine 結束。
- 八個 Provider 各以 16 個併發申請驗證用量查詢單一執行；失敗後經設定重載，再驗證到期放行、用量更新、錯誤清除及下一次查詢排程，整組連跑三次。
- 重連冷卻、期限不被延長、原 Provider 綁定及成功後紀錄回收，每組 100 個循環，連跑三次。測試直接推進紀錄時間，不等待真實的 30 秒。
- 回合到期並清理資料庫後，再更新同來源快取，仍不能恢復已到期配對。

## 最終驗證

- 全專案 `go test -race`：390 項通過，含子測試；13 個有測試的套件通過，其餘 12 個套件沒有測試檔，不代表已有行為測試覆蓋。
- `go vet -buildvcs=false ./...`：通過。
- `go build -buildvcs=false ./...`：通過。
- 本次修改檔案的 `gofmt`：通過。
- 管理頁面 JavaScript：9 個內嵌區塊或獨立檔案語法檢查通過。本次沒有變更畫面配置，沒有執行瀏覽器視覺測試。

測試使用模擬傳輸與本機 HTTP 伺服器，不查詢正式帳號、不消耗模型額度。第一輪定位初始失敗後，其餘測試二進位均透過隔離執行器在獨立暫存工作目錄執行。

本機測試紀錄與隔離執行器位於 `/tmp/lbp-three-rounds.2smGRi/`。最終完整紀錄為 `final-verified.jsonl`；此目錄是暫存檔，不是專案執行所需資料。

最終執行命令：

```sh
go test -buildvcs=false -race -exec 'node /tmp/lbp-three-rounds.2smGRi/isolated-test-exec.cjs' ./... -count=1 -timeout=240s
go vet -buildvcs=false ./...
go build -buildvcs=false ./...
```

## 適用範圍

這些結果驗證本機程式的故障處理與狀態回收，不代表正式上游不會再過載，也不是跨數小時的正式環境壓力測試。工具交付採下游接受完整事件的證據，不等同於客戶端已確認執行。

本次未部署、未發布、未操作 Git。修改前的備份在最終驗證完成後才移除。

## 後續修正的驗證範圍

上述 390 項結果是三輪檢視完成時的紀錄，不涵蓋後續「代理等待冷卻後自動接續」修改。後續已更正將暫時節流當作完成回應的舊斷言，並新增同連線恢復、取消、下游失敗、等待上限及工具防重播情境；程式與測試程式均編譯通過，但尚未執行新增測試或重跑完整測試。最新行為請見 [連線與重試](RETRY_POLICY.md)。
