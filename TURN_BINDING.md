# 對話配對與更新恢復

此功能讓工具接續與重新連線盡量回到原本的 Provider／模型，減少切換帳號造成的前文不相容。

## 配對方式

- 第一次選定來源後，在送出前保存配對；後續同一識別的請求沿用原來源。
- 暫時過載或配額差距不會讓已固定的請求任意更換 Provider。
- 呼叫端提供明確提問回合識別時，同一回合的工具接續與重試應使用相同識別。
- 若沒有提問回合識別，系統保守沿用整段對話的配對，不從文字猜測何時開始新問題。
- 使用舊回應接續的增量請求仍須回到原來源，不能只換識別就把前文移到其他帳號。

配對依呼叫端身分隔離。自訂用戶端可提供 `X-Proxy-Turn-ID`，並在同一回合保持 `prompt_cache_key` 穩定；這是代理的擴充方式，不代表原生 Codex 一定會提供回合標頭。

## 系統更新

透過管理頁面「系統更新」升級部署 ZIP 時：

1. 更新前先保存目前可恢復的對話路由；保存失敗則停止更新。
2. 更新套件不覆蓋既有 `data/`。
3. 新服務啟動後處理 API 請求時載入快照，恢復仍有效的來源配對與綁定數。
4. Provider 帳號已變更、配對過期或無法驗證的紀錄不會直接套用。

這個流程在已安裝支援版本後的後續更新生效。舊版本發起首次升級時，不能執行新版才有的更新前保存功能。

**保存配對不等於保存連線。** 更新仍會中斷進行中的串流，用戶端需要重新連線。建議在較少請求時更新，並先備份設定與資料。DMG／MSI 安裝版請依 [安裝說明](install.md) 升級，不使用管理頁面的 ZIP 更新。

## 配對遺失時

若舊配對不存在，但請求包含使用者前文、完整工具呼叫與結果，且不依賴舊回應參照，系統可重新選擇一次來源，再固定新的配對以恢復問答。

這個恢復方式會捨棄不能跨帳號沿用的推理項目，但保留可見訊息與工具結果；後續恢復請求也不沿用那些推理項目。只有增量資料、工具歷史不完整、帳號變更衝突或保存檔損毀時，不會假裝已恢復。

若仍無法接續，請指定原 Provider 或另開新提問，補充必要前文。恢復並不保證模型不會再次提出同樣的工具操作。

## 保存範圍

配對與更新快照放在進階設定檔所在目錄，預設為 `data/`；不保存 Token、提示詞或工具內容。配對持久化有效期為 30 天，最多 10,000 筆，達容量限制時拒絕新增而不刪除有效配對。

更新快照只恢復路由關係，不恢復活躍請求、程序狀態或 TCP／SSE 連線。回應識別的路由在轉送結束後補存，若程序在補存前中斷，該次增量接續仍可能無法恢復。不同服務實例不應共用同一份可寫資料目錄。

固定來源可減少跨帳號接續問題，不能消除模型內部錯誤、上游過載或網路中斷。等待與重試方式請見 [連線與重試](RETRY_POLICY.md)。
